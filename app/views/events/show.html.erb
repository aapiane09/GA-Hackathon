<!-- SHOW ONE EVENT -->

<div class="container">

  <h2><%= @event.event_name %></h2>

  <%= button_to "Add Idea", new_idea_path(@event), method: "get" %>

  <h2>SUPER COOL IDEAS!</h2>
  <hr>
  <!-- <% @event.ideas.each do |idea| %>
    <h4><%= link_to "#{idea.title}", idea_path(idea)%></h4>
    <p> <%= idea.liked_by idea.user %> </p>
    <p> <%= idea.votes_for.size %> </p>


    <h4><%= "#{idea.content}"%></h4>
    <hr>
  <% end %> -->
  <% @event.ideas.each do |idea| %>
      <h4><%= link_to idea.title, idea %><br>
        Submitted <%= time_ago_in_words(idea.created_at) %>
        by <%= idea.user.username %>
      </h4>
      <%= link_to like_idea_path(idea), method: :put do %>
        Upvote
        <%= idea.get_upvotes.size %>
      <% end %>
  <% end %>
</div>
